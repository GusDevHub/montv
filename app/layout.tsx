import type { Metadata } from "next";
import { Roboto } from "next/font/google";
import "./globals.css";
import { cn } from "@/app/_lib/utils";

const roboto = Roboto({
  subsets: ['latin'],
  weight: ['100', '300', '400', '500', '700', '900'],
  style: ['normal', 'italic'],
  display: 'swap'

});

export const metadata: Metadata = {
  title: "MON TV APP BY Gus Diniz",
  description: "Generated by create next app",
  openGraph: {
    title: "MON TV APP BY Gus Diniz",
    description: "Generated by create next app",
    images: [
      {
        url: "/sports/categoy/premier_league.png",
        width: 1200,
        height: 630,
        alt: "MON TV APP Preview",
      },
    ],
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link
          rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
        />
      </head>
      <body
        className={cn("min-h-screen bg text-white font-sans antialiased",
          roboto.className
        )}
      >
        {children}
      </body>
    </html>
  );
}